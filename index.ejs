<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PRUEBA</title>
</head>
<body>
    <div class="container">
        <form action="/search" method="GET">
            <input type="text" name="name" placeholder="Search for a hero">
            <button type="submit">Search</button>
          </form>
      </div>

    <div class="mosaic">
        <% for (const item of a) { %>
            <a href="/cards?id=<%= item.id %>&image=<%= item.image %>">
                <img src="<%= item.image %>" /></a>
          <% } %>

    
</div>

<style>
.mosaic {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(100px, 1fr));
  grid-gap: 10px;
}

.mosaic img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
</style>
      


</body>
</html>